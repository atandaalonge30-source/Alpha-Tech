{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "devCommand": "vite",
  "framework": "vite",
  "env": {
    "GEMINI_API_KEY": "@gemini_api_key"
  },
  "crons": []
}
